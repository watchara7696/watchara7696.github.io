name: GitHub Pages CI
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: release
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.pages-deploy.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Inject
        env:
          A23D38: ${{ secrets.A23D38 }}
          BAA182: ${{ secrets.BAA182 }}
        run: |
          python3 -c "
          import pathlib, sys, os
          env_a = os.environ.get('A23D38', '')
          env_b = os.environ.get('BAA182', '')
          doc = pathlib.Path('index.html')
          body = doc.read_text(encoding='utf-8')
          result = body
          if env_a:
              result = result.replace('<!-- 918ef28091ed7a09c88a7f48d6daad4dab6e9fc68b708af4 -->', env_a)
          if env_b:
              result = result.replace('<!-- 50d6ec1a2a327f6828a8b4ce154e729caa46 -->', env_b)
          if result == body and (env_a or env_b):
              sys.exit(1)
          if result != body:
              doc.write_text(result, encoding='utf-8')
          "
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: pages-deploy
        uses: actions/deploy-pages@v4
